<div class="column span-12 colborder">
	<h3>Recent Albums Reviews</h3>
	<% for review in @reviews %>
		<div class="front_review">
			<div class="rating"><%= review.score %></div>
			<div class="albumartist"><%= artist_album(review.album) %></div>
			<div class="byline">Reviewed by <%= review.user.name %></div>
			<div class="body"><%= review.truncated_body %></div>
		</div>
	<% end %>
</div>

<div class="column span-11 last">
	<h3>Upcoming Record Releases</h3>
	<dl id="front_calendar">
		<% @release_days = @albums.group_by { |a| a.release_date.beginning_of_day } unless @albums.nil? %>
		<% unless @release_days.nil? %>
			<% @release_days.keys.sort.each do |day| %>
				<dt><%= day.strftime('%B %e')%></dt>
				<% for album in @release_days[day] %>
					<dd><%= link_to artist_album(album), album_path(album) %></dd>
				<% end %>
			<% end %>
		<% end %>
	</dl>
</div>