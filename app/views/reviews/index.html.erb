<%= title "Browse Reviews", :h2 %>

<p><%= will_paginate @reviews %></p>

<% for review in @reviews %>
	<div class="front_review">
		<div class="rating"><%= review.score %></div>
		<div class="albumartist"><%= link_to artist_album(review.album), review_path(review) %></div>
		<div class="genres">Genres: 
			<% for tag in review.album.tags %>
				<%= link_to tag, genre_album_path(tag.name) %>
			<% end %>
		</div>
		<div class="byline">Reviewed by <%= review.user.name %></div>
		<div class="body"><%= review.truncated_body %></div>
		<div class="readmore"><%= link_to 'Read More &gt;', review_path(review) %></div>
	<!--<%= link_to 'Destroy', review, :confirm => 'Are you sure?', :method => :delete %>-->
	</div>

<% end %>

<p><%= will_paginate @reviews %></p>
